/* Test 04 — play_rule with keyword conditions (same_color, same_value, wild)
   Expected: ACCEPT
   Covers: PLAY_CONDITION_SAME_COLOR, PLAY_CONDITION_SAME_VALUE, PLAY_CONDITION_WILD */

game ColorMatch {
    players 2 .. 4
    hand 7
    deck {
        card NumberCard { }
        card SkipCard { }
        card WildCard { }
    }
    play_rule {
        allow if same_color
        allow if same_value
        allow if wild
    }
    win if empty_hand
}
